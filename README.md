# REGEX-FINDER

lack of description (comming soon)

## Example:
Example:
```javascript
const list = ['navbar', 'simple', 'slogan', 'icon-brand-cut']
var notFound = find({
  list: list,
  path: './out/',
  getResumeOf: 'NOT_FOUND'
});
```
